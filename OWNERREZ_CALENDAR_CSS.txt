/* ============================================================
   HIDE HEADER AND FOOTER
   ============================================================ */
   
div.container h1.page-header {
    display: none;
}

/* Month title */
.flex-calendar-month-title {
    display: block;
    color: #1a1610;
    font-size: 1.3rem;
    font-weight: 600;
    margin-bottom: 1.5rem;
    text-align: center;
    padding: 0.5rem 0;
}

/* Footer */
.container > .row:last-child {
    display: none;
}


/* ============================================================
   MONTH NAVIGATOR
   ============================================================ */

div[data-role="month-navigator"] {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;

    width: 100%;
    max-width: 600px;
    margin: 0 auto 1.5rem;
}

div[data-role="month-navigator"] .input-group-addon {
    flex: 1;
    text-align: center;
}

.btn.btn-default.book-cal-previous,
.btn.btn-default.book-cal-today,
.btn.btn-default.book-cal-next,
a.btn.btn-default.book-cal-previous,
a.btn.btn-default.book-cal-today,
a.btn.btn-default.book-cal-next {
    background: rgba(255, 255, 255, 0.7);
    color: #1a1610;
    border: 0.5px solid #d4c9b8;
    border-radius: 8px;
    padding: 0.5rem 0.75rem;
    font-size: 0.95rem;
    font-weight: 500;
    transition: all 0.2s ease;
    text-decoration: none;
    min-width: 80px;
}

.btn.btn-default.book-cal-previous.disabled,
a.btn.btn-default.book-cal-previous.disabled {
    opacity: 0.5;
    cursor: not-allowed;
}


/* ============================================================
   CALENDAR BASE
   ============================================================ */

#calendar.flex-calendar {
    width: 100%;
    margin: 0 auto;
}

#calendar table {
    border-collapse: separate;
    border-spacing: 6px;
    width: 100%;
    table-layout: fixed; /* IMPORTANT */
}

/* Day headers */
th.day-header {
    color: #4a3f2f;
    font-weight: 600;
    font-size: 1rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    padding: 0.75rem 0.5rem;
    text-align: center;
}


/* ============================================================
   🔑 CALENDAR CELL SIZE (THE REAL FIX)
   ============================================================ */

/* Force ALL calendar cells to same size */
#calendar td {
    height: 80px;              /* controls row height */
    padding: 0;
    vertical-align: middle;
}

/* Make booking container fill the cell */
.booking-container {
    display: block;
    position: relative;
    overflow: hidden;
    height: 100%;
    width: 100%;
    padding: 0;

    background: #ffffff;
    color: #1a1610;
    border: 1px solid #d4c9b8;
    border-radius: 10px;
    cursor: pointer;
    transition: all 0.2s ease;
}


/* ============================================================
   DAY CONTENT
   ============================================================ */

/* Make day-content fill the entire cell */
.booking-container .day-content {
    width: 100%;
    height: 100%;
    position: relative;

    display: grid;
    grid-template-rows: auto 1fr auto;
    align-items: center;
    justify-items: center;
    padding: 0.4rem;
}

.day-number {
    color: #1a1610;
    font-size: 1.05rem;
    font-weight: 500;
}

div.day-rate {
    color: #5a8a3a;
    font-weight: 600;
    font-size: 0.9rem;
}

/* Reserve space even when no price */
.booking-container.flex-calendar-out-of-range .day-rate,
.booking-container.day-rate-hidden .day-rate {
    visibility: hidden; /* NOT display:none */
}


/* ============================================================
   STATES
   ============================================================ */

.booking-container:not(.flex-calendar-out-of-range):hover {
    background: rgba(212, 201, 184, 0.15);
    border-color: #5a8a3a;
    transform: translateY(-1px);
}

.booking-container.flex-calendar-out-of-range,
.booking-container.flex-calendar-booking-middle {
    background: #f8f6f2;
    color: #b8a896;
    cursor: not-allowed;
    opacity: 0.6;
}

.booking-container.flex-calendar-today {
    border: 2px solid #5a8a3a;
    font-weight: 600;
}

.booking-container.flex-calendar-holiday {
    background: #DEEBF6;
    border-color: #8796A2;
}


/* ============================================================
   LEGEND
   ============================================================ */

.pull-right.hidden-smallphone,
.visible-smallphone {
    display: block;
    text-align: center;
    margin: 1rem auto;
    padding: 0.75rem;
    background: #f8f6f2;
    border-radius: 8px;
    width: 100%;
}

.pull-right.hidden-smallphone > div,
.visible-smallphone > div {
    display: inline-block;
    margin: 0 0.5rem;
    font-size: 1rem;
}


/* ============================================================
   GENERAL
   ============================================================ */

body {
    font-family: system-ui, -apple-system, BlinkMacSystemFont, 'SF Pro Text',
                 'Segoe UI', sans-serif;
    color: #1a1610;
    background: #ffffff;
}


/* ============================================================
   RESPONSIVE
   ============================================================ */

@media (max-width: 768px) {

    /* Let table rows size naturally */
    #calendar td {
        height: auto;
    }

    /* Compact card */
    .booking-container {
        height: auto;
        max-height: 64px;   /* 🔑 controls pill height */
    }

    /* Use flex instead of grid on mobile */
    .booking-container .day-content {
        display: flex;
        flex-direction: column;
        justify-content: center;
        gap: 4px;
        padding: 6px;
    }

    .day-number {
        font-size: 0.9rem;
        line-height: 1;
    }

    .day-rate {
        font-size: 0.75rem;
        line-height: 1;
        white-space: nowrap;
    }

    th.day-header {
        font-size: 0.9rem;
        padding: 0.5rem 0.25rem;
    }
}